import { EmailValidateDirective } from './email-validator.directive';

describe('EmailValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new EmailValidateDirective();
    expect(directive).toBeTruthy();
  });
});
