import { DecimalSlicePipe } from './decimal-slice.pipe';

describe('DecimalSlicePipe', () => {
  it('create an instance', () => {
    const pipe = new DecimalSlicePipe();
    expect(pipe).toBeTruthy();
  });
});
