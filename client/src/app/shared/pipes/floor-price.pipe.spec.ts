import { FloorPricePipe } from './floor-price.pipe';

describe('FloorPricePipe', () => {
  it('create an instance', () => {
    const pipe = new FloorPricePipe();
    expect(pipe).toBeTruthy();
  });
});
